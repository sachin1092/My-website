$(document).ready(function () {
    function a() {
        for (var a = 0; a < $(".jumbotron").length; a++)$(".jumbotron").eq(a).height($(window).height())
    }

    function d(a) {
        tallest = 0, a.each(function () {
            thisHeight = $(this).height(), thisHeight > tallest && (tallest = thisHeight)
        }), a.height(tallest)
    }

    $(".top").height($(window).height()), $("a[href*=#]:not([href=#])").click(function () {
        if (location.pathname.replace(/^\//, "") == this.pathname.replace(/^\//, "") && location.hostname == this.hostname) {
            var a = $(this.hash);
            if (a = a.length ? a : $("[name=" + this.hash.slice(1) + "]"), a.length)return $("html,body").animate({scrollTop: a.offset().top}, 1e3), !1
        }
    }), $(window).resize(function () {
        $(".top").height($(window).height()), a()
    }), $(window).width() > 568 && (d($(".jumbotron#work .single-item")), d($(".jumbotron#projects .single-item")), a()), $.fx.interval = -2e3;
});