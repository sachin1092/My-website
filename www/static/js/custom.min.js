$(document).ready(function () {

    $(window).resize(function() {

        $('.top').css('height', window.innerHeight);
        console.log(window.innerHeight);

    });

    function a() {
        for (var a = 0; a < $(".jumbotron").length; a++)$(".jumbotron").eq(a).height($(window).height())
    }

    function b() {
        $("#sidebar").addClass("active"), $("#menu-toggle").addClass("active"), $(".page-wrapper").addClass("moveRight")
    }

    function c() {
        $("#sidebar").removeClass("active"), $("#menu-toggle").removeClass("active"), $(".page-wrapper").removeClass("moveRight")
    }
//
//    function d(a) {
//        tallest = 0, a.each(function () {
//            thisHeight = $(this).height(), thisHeight > tallest && (tallest = thisHeight)
//        }), a.height(tallest)
//    }

    $(".top").height($(window).height()), $("a[href*=#]:not([href=#])").click(function () {
        if (location.pathname.replace(/^\//, "") == this.pathname.replace(/^\//, "") && location.hostname == this.hostname) {
            var a = $(this.hash);
            if (a = a.length ? a : $("[name=" + this.hash.slice(1) + "]"), a.length)return $("html,body").animate({scrollTop: a.offset().top}, 1e3), !1
        }
    }), $("#menu-toggle").click(function (a) {
        a.preventDefault(), a.stopPropagation(), $("#sidebar").hasClass("active") ? c() : b()
    }), $(document).click(function () {
        $("#sidebar").hasClass("active") && c()
    }), $("#sidebar a").click(function () {
        setTimeout(c, 500)
    }), $(window).resize(function () {
        $(".top").height($(window).height()), a()
    }), $(window).width() > 568 , $.fx.interval = -2e3;

});